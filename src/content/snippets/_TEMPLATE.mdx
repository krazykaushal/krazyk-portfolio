---
# Copy this file, rename it to the slug you want in the URL, and delete the
# comments. The filename becomes the URL: `debounce.mdx` → /snippets/debounce
#
# The `_` prefix keeps this file out of the collection — the glob pattern in
# src/content.config.ts excludes anything starting with an underscore.

title: "A short name for the thing"

# Optional. One line on what it's for / when you'd reach for it.
description: ""

# REQUIRED, and a closed set. Must be one of:
#   typescript | python | bash | css | sql | astro
# Anything else fails the build on purpose. Add a member to the z.enum in
# src/content.config.ts when you need a new language.
language: typescript

# Free-form. Lowercased automatically. Use these for work/personal, a project
# name, a topic — anything the language axis doesn't capture.
tags: []

addedDate: 2026-08-25

# Optional — only when you've actually changed it since.
# updatedDate: 2026-09-01

# Optional. Where you found it. Must be a valid URL if present.
# source: ['https://example.com/post']

draft: false
---

A sentence or two of context: what problem this solves, and the gotcha worth
remembering. Then the code.

```ts
// the snippet
```

Anything worth saying about the edges goes after.
